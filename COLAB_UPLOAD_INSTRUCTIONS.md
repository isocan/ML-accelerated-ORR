# Upload instructions

Your repository currently has no `notebooks/` directory, so the earlier Colab
URL returned a 404.

Upload this file directly to the repository root:

```text
00_build_candidate_database_colab.ipynb
```

## GitHub steps

1. Open `isocan/ML-accelerated-ORR`.
2. Select **Add file → Upload files**.
3. Upload `00_build_candidate_database_colab_root.ipynb`.
4. Rename it on GitHub to:

   ```text
   00_build_candidate_database_colab.ipynb
   ```

5. Commit directly to `main`.

The working Colab URL will then be:

https://colab.research.google.com/github/isocan/ML-accelerated-ORR/blob/main/00_build_candidate_database_colab.ipynb

GitHub will also display the embedded tables and plots directly in the notebook
page without requiring execution.
